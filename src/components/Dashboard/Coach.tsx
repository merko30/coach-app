import { AthleteQuestionsList } from "./AthleteQuestionsList";
import { StatsBoxes } from "./DashboardWidgets";

const CoachDashboard = () => {
  return (
    <>
      <StatsBoxes />
      <AthleteQuestionsList />
    </>
  );
};

export default CoachDashboard;
